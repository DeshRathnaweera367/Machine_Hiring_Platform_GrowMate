name: growmate_harvester
description: A harvester machine hiring platform for farmers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI Packages
  google_fonts: ^6.3.3
  font_awesome_flutter: ^10.7.0
  flutter_rating_bar: ^4.0.1
  
  # Functionality Packages
  table_calendar: ^3.0.9
  cached_network_image: ^3.4.1
  provider: ^6.1.2
  intl: ^0.19.0
  
  # Image Picker
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # Assets configuration
  assets:
    - assets/images/